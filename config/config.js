module.exports = global.config = {
    // Pagination config: orders items to display on each page
    paginPerItem:4,

    paymentMethods:{
        0: "Credit card",
        1: "Direct bank transfert",
        2: "Cash on delivery",
    }

};